<div class="container">
  <div class="row">
    <div class="col-12">
      <% @items.each do  |item| %>
       <%= item.genre.name %>
       <%= item.item_name %>
       <%= item.price %>
      <% end %>
    </div>
  </div>
</div>